# 编译产物
probe-server
probe-server.exe
probe-agent
probe-agent.exe
/dist-bin/

# 数据库
*.db
*.db-shm
*.db-wal

# 前端
/web/node_modules/
# dist 是构建产物不入库，只保留 .gitkeep 占位让 go:embed 能编译；
# 发布时由 GitHub Actions / build 脚本现场构建。
/web/dist/*
!/web/dist/.gitkeep

# 依赖缓存
/vendor/

# 编辑器
.vscode/
.idea/
.DS_Store
